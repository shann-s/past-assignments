Script started on Tue 10 Nov 2015 04:39:02 PM EST
sasioson@matrix:~/IPC_144/workshops> whoami
sasioson
sasioson@matrix:~/IPC_144/workshops> cat workshop7.c 
#include <stdio.h>
#define MAX 100

void calc_values( double prc[], int qty[], double val[], int num);

void show_table( long long bcode[], double prc[], int qty[], double val[], int num);

int main(void) {
	
	long long barcode[MAX];
	double price[MAX];
	int quantity[MAX];
	double values[MAX];
	int counter;
	long long b;

	counter = 0;

	printf("Grocery Store Inventory\n");
	printf("=======================\n");

	b = 1;

	while (b != 0) {	//Prompts the user for array information

	printf("Barcode  :  ");
	scanf("%lld", &b);

		if (b != 0) {

			barcode[counter] = b;

			printf("Price  :  ");
			scanf("%lf", &price[counter]);

			printf("Quantity  :  ");
			scanf("%d", &quantity[counter]);

			counter = counter + 1;
	
		}
	}

	calc_values(price, quantity, values, counter);		//Calls function to calculate the total of price and quantity

	show_table(barcode, price, quantity, values, counter);		//Prints the Table of the barcode, price, quantity, and value

	return 0;
}

void calc_values( double prc[], int qty[], double val[], int num) {

	int i;

	i = 0;

	for(i = 0; i <  num; i++) {	//Calculates each price and quantity in the array

	val[i] = prc[i] * qty[i];

	}

}

void show_table( long long bcode[], double prc[], int qty[], double val[], int num) {

	int a;
	
	a = 0;

	printf("          Goods in Stock\n");
	printf("          ==============\n");

	printf("Barcode      Price      Quantity   Value\n");
	printf("----------------------------------------\n");

	for(a = 0; a < num; a++) {		//Prints all the information collected and calculated

	printf("%lld   %.2lf        %d        %.2lf\n", bcode[a], prc[a], qty[a], val[a]);

	}

}
sasioson@matrix:~/IPC_144/workshops> gcc workshop7.c 
sasioson@matrix:~/IPC_144/workshops> a.out
Grocery Store Inventory
=======================
Barcode  :  6300090801
Price  :  3.15  25
Quantity  :  5
Barcode  :  6380613916
Price  :  4.45
Quantity  :  15
Barcode  :  6038367157
Price  :  3.95
Quantity  :  10
Barcode  :  0
          Goods in Stock
          ==============
Barcode      Price      Quantity   Value
----------------------------------------
6300090801   3.25        5        16.25
6380613916   4.45        15        66.75
6038367157   3.95        10        39.50
sasioson@matrix:~/IPC_144/workshops> exit
exit

Script done on Tue 10 Nov 2015 04:40:49 PM EST
