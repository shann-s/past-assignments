#include <stdio.h>

int main(void) {

	int temp, a, b, i;

	a = 10;

	b = 21;

	printf("a: %d, b: %d, temp: %d\n", a, b, temp);

	temp = a;

	a = b;

	b = temp;

	temp = (a + 100 * b) / 2;

	printf ("a: %d, b: %d, temp: %d\n", a, b, temp);

	printf("Enter an integer : ");
	scanf("%d", a);

	while ( a < 999999) {

	printf

	}	

	return 0;
}
